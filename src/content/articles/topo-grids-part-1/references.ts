import type {
  ReferenceRecord,
} from '@/content/references/types';

/**
 * References used by Part I.
 *
 * Keep each citation exactly as approved for publication.
 * The order displayed in the article is controlled by
 * article.referenceIds, not by this array.
 */
export const topoGridsPart1References:
  readonly ReferenceRecord[] = [];
